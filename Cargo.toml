[workspace]
resolver = "2"
members = [
    "common",
    "lua",
    "uefi",
    "arm64-bare",
    "romwrap",
    "bios",
    "native",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
